# Local state (each cloud dir keeps its own state file)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform working directory (one per cloud dir: aws/.terraform, ...)
.terraform/
.terraform.lock.hcl

# Crash / plan artifacts
crash.log
crash.*.log
*.tfplan
