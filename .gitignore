# Local state (this project uses local state only, one file per cloud)
state/
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Crash / plan artifacts
crash.log
crash.*.log
*.tfplan
